import { mockRequest, mockResponse } from "../utils/mock";
import * as productController from "../../api/controller/productController";

const mockProduct = {
    
}

describe("get product by Id", () => {

})